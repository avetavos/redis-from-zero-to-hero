---
title: Expiration & TTL
description: 'Set keys to auto-delete with EXPIRE, check remaining time with TTL, and understand how Redis removes expired keys.'
sidebar:
  order: 3
---

import TryRedis from '../../../../components/TryRedis.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Why expiration matters

Redis is often used as a cache or a session store. In both cases you want keys to disappear automatically after some time — you should not have to write cleanup jobs. Redis handles this natively through **key expiration**.

## Setting an expiration

You can attach a TTL (time-to-live) when you create a key, or add one to an existing key:

| Command | Description |
|---------|-------------|
| `SET key val EX secs` | Create + expire in seconds |
| `SET key val PX ms` | Create + expire in milliseconds |
| `EXPIRE key secs` | Set/update expiry in seconds on existing key |
| `PEXPIRE key ms` | Set/update expiry in milliseconds |
| `EXPIREAT key unix-secs` | Expire at an absolute Unix timestamp (seconds) |
| `PEXPIREAT key unix-ms` | Expire at an absolute Unix timestamp (milliseconds) |

```text
127.0.0.1:6379> SET session:abc "user:42" EX 300
OK
127.0.0.1:6379> TTL session:abc
(integer) 299
127.0.0.1:6379> SET cache:home "<html>...</html>"
OK
127.0.0.1:6379> EXPIRE cache:home 60
(integer) 1
127.0.0.1:6379> TTL cache:home
(integer) 59
```

export const expireCode = `SET session:abc "user:42" EX 300
TTL session:abc
SET cache:home "<html>...</html>"
EXPIRE cache:home 60
TTL cache:home`;

<TryRedis client:visible code={expireCode} />

## TTL and PTTL — checking remaining time

`TTL` returns seconds; `PTTL` returns milliseconds. Both have two special return values:

- `-1` — the key exists but has **no expiry** (it is persistent)
- `-2` — the key **does not exist** (or has already expired)

```text
127.0.0.1:6379> SET persistent "I live forever"
OK
127.0.0.1:6379> TTL persistent
(integer) -1
127.0.0.1:6379> TTL nonexistent:key
(integer) -2
127.0.0.1:6379> SET brief "gone soon" PX 5000
OK
127.0.0.1:6379> PTTL brief
(integer) 4987
```

export const ttlCode = `SET persistent "I live forever"
TTL persistent
TTL nonexistent:key
SET brief "gone soon" PX 5000
PTTL brief`;

<TryRedis client:visible code={ttlCode} />

## PERSIST — removing an expiration

If you want to cancel a key's expiry and make it persistent again, use `PERSIST`:

```text
127.0.0.1:6379> SET promo "SALE10" EX 3600
OK
127.0.0.1:6379> TTL promo
(integer) 3599
127.0.0.1:6379> PERSIST promo
(integer) 1
127.0.0.1:6379> TTL promo
(integer) -1
```

export const persistCode = `SET promo "SALE10" EX 3600
TTL promo
PERSIST promo
TTL promo`;

<TryRedis client:visible code={persistCode} />

## How Redis actually removes expired keys

Redis uses two complementary strategies — you do not need to do anything, but understanding them helps you reason about memory:

**Lazy expiry** — when you access a key (GET, EXISTS, etc.), Redis checks its expiry first. If it has expired, Redis deletes it right then and returns `(nil)` as if it never existed.

**Active expiry** — Redis runs a background cycle (by default ~10 times per second) that randomly samples keys with TTLs and deletes the ones that have expired. This prevents the keyspace from filling up with keys that are never accessed.

Together these strategies keep memory usage bounded without blocking your application.

<Callout title="TTL returns -1 or -2 — know the difference">

`TTL key` has three possible outcomes:

- A positive integer — seconds remaining until expiry
- `-1` — the key exists and is **persistent** (no expiry set)
- `-2` — the key **does not exist** at all

A common bug is checking `TTL key == 0` to detect missing keys. It is never 0; once the clock hits zero, the key is already gone and you get `-2`. Always branch on `-2` for missing and `-1` for persistent.

</Callout>

## Trade-offs

| Choice | Benefit | Cost |
|---|---|---|
| Set a TTL on every cache/session key | Memory is reclaimed automatically, no cleanup job to write | A miscalculated TTL can expire data too early while the system still needs it |
| Rely on lazy expiry (checked on access) | No extra overhead if nobody ever reads that key | An expired key nobody accesses keeps consuming memory until the active-expiry cycle finds it |
| Use `PERSIST` to cancel an existing TTL | Extends the life of important data without creating a new key | Easy to forget the key was meant to be temporary, letting memory grow unchecked |

## Common mistakes

- **Checking `TTL key == 0` to detect a missing key** — the correct value for a nonexistent key is `-2`, never `0`, because TTL never sits at zero. Branch on `-2` (missing) and `-1` (persistent) correctly.
- **Forgetting to set a TTL on a key that should be temporary** — if you `SET` a cache or session value with no `EX`/`PX`, it becomes persistent and accumulates until `maxmemory` is full.
- **Confusing `EXPIRE` (relative) with `EXPIREAT` (absolute timestamp)** — using the wrong one shifts the TTL away from what you intended, especially when the client and server clocks disagree.

> 💡 Real-world example
>
> **Stack Overflow** ran an L1/L2 cache (local memory plus Redis) in front of SQL Server at a reported scale of roughly 160 billion Redis operations a month ([Stack Overflow: The Architecture – 2016 Edition](https://nickcraver.com/blog/2016/02/17/stack-overflow-the-architecture-2016-edition/), Nick Craver, 2016). Expiring cached values with a TTL, as shown in this lesson, is exactly the mechanism that keeps that kind of cache from serving stale data forever.
>
> **General pattern** — session stores commonly attach a TTL to the session key so an inactive user is logged out automatically, instead of running a background job to sweep old sessions.

export const quizTTL = [
  {
    q: "What does TTL return for a key that exists but has no expiration set?",
    options: ["0", "-1", "-2", "nil"],
    answer: 1,
  },
  {
    q: "Which command cancels an expiration and makes a key persistent?",
    options: ["UNPERSIST", "PERSIST", "EXPIRE key -1", "SETPERSIST"],
    answer: 1,
  },
  {
    q: "What does PEXPIRE set compared to EXPIRE?",
    options: [
      "Expiry in minutes instead of seconds",
      "Expiry in milliseconds instead of seconds",
      "Expiry at an absolute timestamp",
      "Expiry relative to last access time",
    ],
    answer: 1,
  },
  {
    q: "What does lazy expiry mean in Redis?",
    options: [
      "Redis never actually deletes expired keys",
      "Redis checks expiry only when a key is accessed",
      "Redis checks expiry only on writes, not reads",
      "Redis deletes expired keys in batches every minute",
    ],
    answer: 1,
  },
];

<Quiz client:visible id="keys-strings/expiration-ttl" questions={quizTTL} />

<ProgressTracker client:visible id="keys-strings/expiration-ttl" />
