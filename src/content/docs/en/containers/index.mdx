---
title: 'Running Containers'
description: 'Overview of the container lifecycle — created, running, stopped, and removed — and what this module covers.'
sidebar:
  order: 1
---

import PlayWithDocker from '../../../../components/PlayWithDocker.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## What is a container?

An image is a blueprint. A **container** is a live, running instance of that blueprint — an isolated process with its own filesystem, network, and process space, all sharing the host kernel.

You can run many containers from the same image, stop and restart them, inspect their logs, and remove them when you are done. Understanding this lifecycle is the foundation of working with Docker day to day.

## The container lifecycle

Every container moves through four states:

| State | What it means |
|-------|---------------|
| **Created** | `docker create` allocated the container but it has not started yet. |
| **Running** | The container's main process is executing. |
| **Stopped** | The main process exited (or you called `docker stop`). The container still exists on disk. |
| **Removed** | `docker rm` deleted the container and freed its writable layer. |

```text
docker run   →  Created + Running
docker stop  →  Stopped
docker start →  Running again
docker rm    →  Removed (gone)
```

## What this module covers

| Lesson | Topic |
|--------|-------|
| **Lifecycle** | `docker run`, `docker ps`, `docker stop/start/restart`, `docker rm`, `--rm` |
| **Exec & Logs** | `docker exec -it`, `docker logs`, `docker top`, attaching |
| **Ports & Env** | Publishing ports `-p`, environment variables `-e`, naming `--name` |
| **Restart & Inspect** | Restart policies, `docker inspect`, `docker stats`, resource limits |

## Quick demo — run, list, and stop

The three commands you will use most in this module:

```bash
# Pull and run nginx in the background
docker run -d --name demo nginx:alpine

# List running containers
docker ps

# Stop it
docker stop demo

# Remove it
docker rm demo
```

```text
CONTAINER ID   IMAGE          COMMAND                  CREATED        STATUS        PORTS     NAMES
a1b2c3d4e5f6   nginx:alpine   "/docker-entrypoint.…"   3 seconds ago  Up 2 seconds  80/tcp    demo
```

export const containerOverviewCode = `# Pull and run nginx detached
docker run -d --name demo nginx:alpine

# Confirm it is running
docker ps

# Stop the container
docker stop demo

# Remove it
docker rm demo

# Confirm it is gone
docker ps -a`;

<PlayWithDocker client:visible code={containerOverviewCode} label="Run your first container" />

export const quizContainersIndex = [
  {
    q: "Which state does a container enter immediately after 'docker stop'?",
    options: [
      "Removed",
      "Paused",
      "Stopped",
      "Created"
    ],
    answer: 2
  },
  {
    q: "What is the difference between an image and a container?",
    options: [
      "An image is a running process; a container is a static file",
      "An image is a blueprint; a container is a running instance of that blueprint",
      "They are the same thing with different names",
      "A container is built with docker build; an image is started with docker run"
    ],
    answer: 1
  },
  {
    q: "After 'docker stop', can you start the container again?",
    options: [
      "No — stopped containers are automatically removed",
      "Only if you used --rm when creating it",
      "Yes — use 'docker start <name>'",
      "Only by rebuilding the image"
    ],
    answer: 2
  }
];

<Quiz client:visible id="containers/index" questions={quizContainersIndex} />

<ProgressTracker client:visible id="containers/index" />
