---
title: Set Operations
description: 'SINTER, SUNION, SDIFF and their STORE variants — common friends, audience overlap, and server-side set algebra.'
sidebar:
  order: 3
---

import TryRedis from '../../../../components/TryRedis.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Server-side set algebra

Redis can compute **intersection**, **union**, and **difference** between two or more Sets entirely on the server. You never need to pull all members to the client and compute locally.

## SINTER — intersection

`SINTER key [key ...]` returns only the members that appear in **every** listed Set. Classic use case: mutual friends.

```text
127.0.0.1:6379> SADD user:1:follows "alice" "bob" "carol" "dave"
(integer) 4
127.0.0.1:6379> SADD user:2:follows "bob" "carol" "eve" "frank"
(integer) 4
127.0.0.1:6379> SINTER user:1:follows user:2:follows
1) "bob"
2) "carol"
```

export const sinterCode = `SADD user:1:follows "alice" "bob" "carol" "dave"
SADD user:2:follows "bob" "carol" "eve" "frank"
SINTER user:1:follows user:2:follows`;

<TryRedis client:visible code={sinterCode} />

## SUNION — union

`SUNION key [key ...]` returns all members from **any** of the listed Sets, deduplicated. Use case: combined audience across campaigns.

```text
127.0.0.1:6379> SADD campaign:A "user:1" "user:2" "user:3"
(integer) 3
127.0.0.1:6379> SADD campaign:B "user:2" "user:4" "user:5"
(integer) 3
127.0.0.1:6379> SUNION campaign:A campaign:B
1) "user:1"
2) "user:2"
3) "user:3"
4) "user:4"
5) "user:5"
```

export const sunionCode = `SADD campaign:A "user:1" "user:2" "user:3"
SADD campaign:B "user:2" "user:4" "user:5"
SUNION campaign:A campaign:B`;

<TryRedis client:visible code={sunionCode} />

## SDIFF — difference

`SDIFF key [key ...]` returns members in the **first** Set that do not appear in any subsequent Set. Use case: find users in one group but not another.

```text
127.0.0.1:6379> SADD plan:premium "user:1" "user:2" "user:3"
(integer) 3
127.0.0.1:6379> SADD notified:promo "user:2" "user:3"
(integer) 2
127.0.0.1:6379> SDIFF plan:premium notified:promo
1) "user:1"
```

export const sdiffCode = `SADD plan:premium "user:1" "user:2" "user:3"
SADD notified:promo "user:2" "user:3"
SDIFF plan:premium notified:promo`;

<TryRedis client:visible code={sdiffCode} />

## SINTERSTORE, SUNIONSTORE, SDIFFSTORE

The `*STORE` variants write the result to a **destination key** instead of returning it. The destination is a regular Set you can inspect later.

```text
127.0.0.1:6379> SADD group:eng "alice" "bob" "carol"
(integer) 3
127.0.0.1:6379> SADD group:ops "carol" "dave"
(integer) 2
127.0.0.1:6379> SINTERSTORE overlap:eng-ops group:eng group:ops
(integer) 1
127.0.0.1:6379> SMEMBERS overlap:eng-ops
1) "carol"
127.0.0.1:6379> SUNIONSTORE all:staff group:eng group:ops
(integer) 4
127.0.0.1:6379> SMEMBERS all:staff
1) "alice"
2) "bob"
3) "carol"
4) "dave"
```

export const storeCode = `SADD group:eng "alice" "bob" "carol"
SADD group:ops "carol" "dave"
SINTERSTORE overlap:eng-ops group:eng group:ops
SMEMBERS overlap:eng-ops
SUNIONSTORE all:staff group:eng group:ops
SMEMBERS all:staff`;

<TryRedis client:visible code={storeCode} />

<Callout title="Set algebra runs server-side — no data transfer needed">

With `SINTER`, `SUNION`, and `SDIFF`, Redis does all the computation in-process. For large Sets (millions of members), this avoids transferring the entire collection to your application server just to intersect or diff it locally. The `*STORE` variants go further: the result is materialized as a new key that other clients can read immediately.

</Callout>

## Trade-offs

| Choice | Benefit | Cost |
|---|---|---|
| Compute set algebra server-side (`SINTER`/`SUNION`/`SDIFF`) | No need to ship every member to the client to compute it yourself — much faster on large Sets | These commands block the server while computing; a very large Set can affect other clients' latency |
| `*STORE` variants (`SINTERSTORE`, etc.) | Materializes the result as a new key you can re-read instantly without recomputing | The result is a snapshot at run time — if the source Sets change later, the result key does not update automatically |
| Using a Set instead of a SQL JOIN for a simple relationship | Avoids a slow JOIN on large tables | Only fits simple many-to-many relationships, not complex relational queries |

## Common mistakes

- **Calling `SINTER`/`SUNION`/`SDIFF` repeatedly on every request instead of caching the result** — if the result doesn't change often, use a `*STORE` variant to save it into a key with a TTL instead of recomputing every time.
- **Forgetting that `SDIFF` cares about the order of the keys you pass** — `SDIFF A B` and `SDIFF B A` give different results, since the first Set is the base you're subtracting from. Swapping the order flips the result unintentionally.
- **Using a `*STORE` variant against a destination key that already holds other data** — these commands overwrite the destination key entirely. If that key was being used for something else, that data is gone immediately.

> 💡 Real-world example
>
> **General pattern** — social platforms commonly use `SINTER` between two users' follower Sets to find mutual friends in real time, without a heavy JOIN against a relational database. Marketing teams commonly use `SUNION`/`SINTERSTORE` to combine or filter audience segments from multiple campaigns on the server, before sending the result to a notification pipeline.

export const quizSetOps = [
  {
    q: "Which command returns members present in ALL listed sets?",
    options: ["SUNION", "SDIFF", "SINTER", "SMEMBERS"],
    answer: 2,
  },
  {
    q: "What does SDIFF key1 key2 return?",
    options: [
      "Members in key2 but not key1",
      "Members in key1 but not key2",
      "Members in both key1 and key2",
      "All members from key1 and key2",
    ],
    answer: 1,
  },
  {
    q: "What is the effect of SINTERSTORE destination key1 key2?",
    options: [
      "Reads the intersection into a local variable",
      "Computes the intersection and stores it in destination",
      "Copies key1 and key2 into destination",
      "Returns the intersection without storing it",
    ],
    answer: 1,
  },
  {
    q: "SUNION across two sets with some overlapping members returns how many copies of a shared member?",
    options: ["Two", "Zero", "One", "It depends on insertion order"],
    answer: 2,
  },
];

<Quiz client:visible id="sets-sorted-sets/set-operations" questions={quizSetOps} />

<ProgressTracker client:visible id="sets-sorted-sets/set-operations" />
