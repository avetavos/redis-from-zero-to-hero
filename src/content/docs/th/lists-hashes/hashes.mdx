---
title: 'Hashes'
description: 'HSET, HGET, HMGET, HGETALL, HDEL, HEXISTS, HKEYS, HVALS, และ HLEN — เก็บคู่ field-value หลายรายการไว้ภายใต้ key เดียว'
sidebar:
  order: 4
---

import TryRedis from '../../../../components/TryRedis.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Hashes — dictionary แบบ flat ภายใต้ key เดียว

Redis Hashes เก็บคู่ field-value หลายรายการไว้ภายใต้ key เดียว ลองนึกภาพเป็น dictionary แบบ flat หรือแถวข้อมูลในฐานข้อมูล — หนึ่ง key เก็บได้หลาย field ที่มีชื่อแยกกัน

## HSET และ HGET

`HSET key field value [field value ...]` ตั้งค่าหนึ่ง field หรือหลาย field พร้อมกัน Redis 7+ รองรับการระบุคู่ field-value หลายชุดในคำสั่งเดียว `HGET key field` ดึงค่าของ field หนึ่งตามชื่อ หาก field ไม่มีอยู่ Redis จะคืนค่า `(nil)`

```text
127.0.0.1:6379> HSET product:1 name "Redis Mug" price "12.99" stock "200"
(integer) 3
127.0.0.1:6379> HGET product:1 name
"Redis Mug"
127.0.0.1:6379> HGET product:1 price
"12.99"
127.0.0.1:6379> HGET product:1 missing-field
(nil)
```

export const hsetGetCode = `HSET product:1 name "Redis Mug" price "12.99" stock "200"
HGET product:1 name
HGET product:1 price
HGET product:1 missing-field`;

<TryRedis client:visible code={hsetGetCode} />

## HMGET และ HGETALL

`HMGET key field [field ...]` ดึงหลาย field ในครั้งเดียว และคืนค่า `(nil)` สำหรับ field ที่ไม่มีอยู่ `HGETALL key` คืนค่าคู่ field-value ทั้งหมดในรูปแบบรายการสลับกันระหว่างชื่อ field และค่า

```text
127.0.0.1:6379> HMGET product:1 name stock
1) "Redis Mug"
2) "200"
127.0.0.1:6379> HGETALL product:1
1) "name"
2) "Redis Mug"
3) "price"
4) "12.99"
5) "stock"
6) "200"
```

export const hmgetCode = `HMGET product:1 name stock
HGETALL product:1`;

<TryRedis client:visible code={hmgetCode} />

## HDEL และ HEXISTS

`HDEL key field [field ...]` ลบหนึ่ง field หรือหลาย field และคืนค่าจำนวน field ที่ถูกลบจริง `HEXISTS key field` คืนค่า `1` หาก field นั้นมีอยู่ หรือ `0` หากไม่มี — เหมาะสำหรับตรวจสอบเงื่อนไขโดยไม่ต้องดึงค่า

```text
127.0.0.1:6379> HEXISTS product:1 price
(integer) 1
127.0.0.1:6379> HEXISTS product:1 color
(integer) 0
127.0.0.1:6379> HDEL product:1 price
(integer) 1
127.0.0.1:6379> HEXISTS product:1 price
(integer) 0
```

export const hdelCode = `HSET product:1 name "Redis Mug" price "12.99" stock "200"
HEXISTS product:1 price
HEXISTS product:1 color
HDEL product:1 price
HEXISTS product:1 price
HKEYS product:1
HVALS product:1
HLEN product:1`;

<TryRedis client:visible code={hdelCode} />

## HKEYS, HVALS, และ HLEN

`HKEYS key` คืนชื่อ field ทั้งหมด `HVALS key` คืนค่าของ field ทั้งหมด `HLEN key` คืนจำนวน field ทั้งหมดใน hash หลังจากลบ `price` ออกแล้ว hash นี้เหลืออยู่ 2 field คือ `name` และ `stock`

```text
127.0.0.1:6379> HKEYS product:1
1) "name"
2) "stock"
127.0.0.1:6379> HVALS product:1
1) "Redis Mug"
2) "200"
127.0.0.1:6379> HLEN product:1
(integer) 2
```

## ตั้งเวลาหมดอายุรายตัวให้แต่ละ field

`EXPIRE` ทำงานกับทั้ง key ดังนั้นเป็นเวลานานที่ hash เป็นแบบทั้งหมดหรือไม่เอาเลย: ทุก field เกิดและตายพร้อมกัน Redis 7.4 เปลี่ยนเรื่องนี้ด้วย **field-level TTL** และ Redis 8 เพิ่มคำสั่งอีกสามตัวที่รวม TTL เข้าไปในการอ่านหรือเขียนเลย

`HEXPIRE key seconds [NX | XX | GT | LT] FIELDS numfields field [field ...]` ใช้ตั้ง TTL สังเกต keyword `FIELDS` และ **จำนวน** field ก่อนรายชื่อ — ทุกคำสั่งในตระกูลนี้ต้องใส่ ค่าที่คืนมาคือหนึ่งตัวเลขต่อหนึ่ง field: `1` เมื่อตั้ง TTL สำเร็จ, `-2` เมื่อไม่มี field นั้น, `0` เมื่อเงื่อนไข `NX`/`XX`/`GT`/`LT` ไม่ผ่าน `HTTL` อ่านวินาทีที่เหลือ และ `HPERSIST` ลบ TTL ออก

```text
127.0.0.1:6379> HSET session:42 user "ana" csrf "t0ken"
(integer) 2
127.0.0.1:6379> HEXPIRE session:42 600 FIELDS 1 csrf
1) (integer) 1
127.0.0.1:6379> HTTL session:42 FIELDS 2 csrf user
1) (integer) 600
2) (integer) -1
```

`-1` หมายถึง field นั้นไม่มีวันหมดอายุ เมื่อ TTL หมด field จะหายไปเอง และเมื่อ field สุดท้ายหายไป key ก็หายตามไปด้วย

### HGETEX, HSETEX และ HGETDEL

คำสั่งสามตัวของ Redis 8 นี้แทนที่ชุดคำสั่งหลายขั้นตอนได้ตัวละชุด:

| คำสั่ง | รูปแบบ | แทนที่ |
|---------|-------|----------|
| `HGETEX key [EX seconds \| PERSIST] FIELDS numfields field [field ...]` | อ่านแล้วต่ออายุ TTL | `HGET` + `HEXPIRE` |
| `HSETEX key [FNX \| FXX] [EX seconds \| KEEPTTL] FIELDS numfields field value [field value ...]` | เขียนพร้อม TTL | `HSET` + `HEXPIRE` |
| `HGETDEL key FIELDS numfields field [field ...]` | อ่านแล้วลบ | `HGET` + `HDEL` |

`HGETEX` คือเครื่องมือพื้นฐานของ sliding session: อ่านค่า field แล้วเลื่อนเวลาหมดอายุออกไปใน call เดียวแบบ atomic `HSETEX` รับ `FNX` (ตั้งเฉพาะเมื่อยังไม่มี field เหล่านั้นเลย) หรือ `FXX` (ตั้งเฉพาะเมื่อมีครบทุก field) พร้อม `KEEPTTL` เพื่อคง TTL เดิมไว้ ส่วน `HGETDEL` คือการอ่านครั้งเดียวจบสำหรับค่าที่ใช้แล้วทิ้ง เช่น one-time token

```text
127.0.0.1:6379> HSETEX session:42 EX 900 FIELDS 1 csrf "fresh-token"
(integer) 1
127.0.0.1:6379> HGETEX session:42 EX 900 FIELDS 1 csrf
1) "fresh-token"
127.0.0.1:6379> HGETDEL session:42 FIELDS 1 csrf
1) "fresh-token"
127.0.0.1:6379> HGET session:42 csrf
(nil)
```

export const hashTtlCode = `HSET session:42 user "ana" csrf "t0ken"
HEXPIRE session:42 600 FIELDS 1 csrf
HTTL session:42 FIELDS 2 csrf user
HSETEX session:42 EX 900 FIELDS 1 csrf "fresh-token"
HGETEX session:42 EX 900 FIELDS 1 csrf
HGETDEL session:42 FIELDS 1 csrf
HGET session:42 csrf`;

<TryRedis client:visible code={hashTtlCode} />

<Callout title="A hash stores an object's fields under one key — memory-efficient">

Redis ใช้การเข้ารหัสแบบกระชับ (listpack) สำหรับ hash ขนาดเล็กโดยอัตโนมัติ แทนที่จะเก็บ `product:1:name`, `product:1:price`, และ `product:1:stock` เป็นสาม string key แยกกัน การใช้ `HSET product:1 ...` เพียงครั้งเดียวจะเก็บทั้งหมดไว้ภายใต้ key เดียว — ประหยัดหน่วยความจำ ลด round-trip และจัดกลุ่มข้อมูลได้อย่างมีตรรกะ

</Callout>

## ข้อแลกเปลี่ยน

| ตัวเลือก | Benefit | Cost |
|---|---|---|
| Hash (field แยกเป็น field-value) | อ่าน/เขียนทีละ field ได้โดยไม่ต้อง parse ทั้งก้อน (`HGET`/`HSET`) | ไม่มี nested structure ในตัว ต้อง flatten ข้อมูลที่ซับซ้อนเอง |
| String เก็บ JSON blob | เก็บโครงสร้างซับซ้อน/nested ได้ตามธรรมชาติ | ต้อง `GET` ทั้งก้อนแล้ว parse ทุกครั้ง แม้ต้องการแค่ field เดียว เขียนทับทั้งก้อนเสมอ |
| `HDEL` ลบทีละ field | ลบเฉพาะ field ที่ไม่ต้องการได้ตรง ๆ โดยไม่กระทบ field อื่น | ต้องรู้ชื่อ field ล่วงหน้า ไม่มี wildcard delete ในตัว |

## ข้อผิดพลาดที่พบบ่อย

- **เก็บ object เป็น JSON string แทนที่จะใช้ hash เมื่อ field ส่วนใหญ่เป็น flat key-value** — เสีย round-trip parse/serialize ทุกครั้งที่อ่านหรือเขียนแค่ field เดียว
- **ใช้ `HGETALL` กับ hash ที่มี field จำนวนมากบ่อย ๆ** — โหลดทุก field เข้าหน่วยความจำ client ทั้งที่ต้องการแค่ 1-2 field ควรใช้ `HMGET` หรือ `HGET` แทน
- **ลืมว่า `HDEL` คืนค่าเป็นจำนวน field ที่ถูกลบจริง ไม่ใช่ boolean** — เข้าใจผิดว่า return `0` คือ error ทั้งที่จริงหมายถึง field นั้นไม่มีอยู่ตั้งแต่แรก

> 💡 ตัวอย่างจากของจริง
>
> **Shopify** — เก็บ attribute ของสินค้า (price, stock, name) เป็น Redis hash ต่อหนึ่ง product ทำให้อัปเดต stock ด้วย field เดียวได้เร็วโดยไม่กระทบ field อื่น
>
> **Twitch** — ใช้ hash เก็บ state ของ stream (viewer count, title, category) ต่อหนึ่ง channel เพื่อให้ dashboard อ่านและอัปเดตแต่ละ field แยกกันแบบ real-time

export const quizHashes = [
  {
    q: "คำสั่งใดที่คืนค่าคู่ field-value ทั้งหมดของ hash?",
    options: ["HGET", "HMGET", "HGETALL", "HKEYS"],
    answer: 2,
  },
  {
    q: "`HGET key missing-field` จะคืนค่าอะไร?",
    options: ["สตริงว่าง", "(nil)", "ข้อผิดพลาด", "0"],
    answer: 1,
  },
  {
    q: "`HSET user:1 name 'Ana' age '30'` สร้าง field กี่ field?",
    options: ["1", "2", "4", "ขึ้นอยู่กับว่า field มีอยู่แล้วหรือไม่"],
    answer: 1,
  },
  {
    q: "คำสั่งใดนับจำนวน field ใน hash?",
    options: ["HCOUNT", "HLEN", "HSIZE", "HHOW"],
    answer: 1,
  },
  {
    q: "ต้องการอ่านค่า field ของ session แล้วเลื่อนเวลาหมดอายุออกไปใน call เดียวแบบ atomic ใช้คำสั่งใด?",
    options: ["HGET ตามด้วย HEXPIRE", "HGETEX", "HGETDEL", "HSETEX"],
    answer: 1,
  },
];

<Quiz client:visible id="lists-hashes/hashes" questions={quizHashes} />

<ProgressTracker client:visible id="lists-hashes/hashes" />
