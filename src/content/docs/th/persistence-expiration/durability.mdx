---
title: การแลกเปลี่ยนด้าน Durability
description: 'โหมด appendfsync (always, everysec, no), ข้อมูลที่อาจสูญหายเมื่อ crash และการรวม persistence กับ replication'
sidebar:
  order: 3
---

import TryRedis from '../../../../components/TryRedis.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## การตั้งค่า appendfsync

ความทนทานของ AOF ขึ้นอยู่กับความถี่ที่ Redis flush AOF buffer ลงดิสก์ directive `appendfsync` ควบคุมสิ่งนี้ด้วยสามโหมด:

- `always`: fsync หลังทุกคำสั่ง — ไม่สูญหายข้อมูล, throughput ต่ำที่สุด
- `everysec`: fsync ทุกวินาที — สูญเสียข้อมูลได้มากที่สุด 1 วินาที, สมดุลที่ดี (แนะนำ)
- `no`: OS ตัดสินใจเองว่าจะ flush เมื่อใด — throughput สูงที่สุด, การสูญหายของข้อมูลไม่แน่นอน

```ini
# Maximum durability — every write fsynced
appendfsync always

# Balance — at most 1 second of data loss (recommended)
appendfsync everysec

# Fastest — OS controls flushing, unpredictable loss
appendfsync no
```

## สิ่งที่อาจสูญหายเมื่อเกิด crash

| โหมด | ข้อมูลสูญหายสูงสุด | Throughput |
|------|--------------|------------|
| `always` | 0 คำสั่ง | ต่ำที่สุด |
| `everysec` | ~1 วินาที | ดี |
| `no` | ตั้งแต่ OS flush ล่าสุด | สูงที่สุด |
| RDB only | ตั้งแต่ snapshot ล่าสุด | สูงที่สุด |

## การรวม persistence กับ replication

การรัน replica ไม่ได้แทนที่ความจำเป็นของ persistence การรัน replica ที่ crash และ restart จะโหลดจากไฟล์ persistence ของตัวเอง (หรือ sync จาก primary) แนวทางที่ดีที่สุด: เปิดใช้ persistence บน replica อย่างอิสระ ไม่ใช่แค่บน primary เท่านั้น

```text
127.0.0.1:6379> CONFIG GET appendfsync
1) "appendfsync"
2) "everysec"
127.0.0.1:6379> CONFIG SET appendfsync always
OK
127.0.0.1:6379> CONFIG GET appendfsync
1) "appendfsync"
2) "always"
```

export const durabilityCode = `CONFIG GET appendfsync
CONFIG SET appendfsync always
CONFIG GET appendfsync`;

<TryRedis client:visible code={durabilityCode} />

<Callout title="appendfsync everysec — สมดุลที่นิยมใช้">

การ deploy ใน production ส่วนใหญ่ใช้ `appendfsync everysec` คุณจะได้ความทนทานของ AOF โดยสูญเสียข้อมูลได้มากที่สุด 1 วินาทีเมื่อ crash และ throughput ยังคงสูง ใช้ `always` เฉพาะเมื่อการไม่สูญหายข้อมูลเลยเป็นข้อกำหนดที่เข้มงวด

</Callout>

<Callout title="AOF ปิดอยู่โดยค่าเริ่มต้น และ AOF สมัยใหม่เป็น directory ไม่ใช่ไฟล์เดียว">

`appendonly` มีค่าเริ่มต้นเป็น `no` — Redis instance ใหม่จะ persist ผ่าน RDB snapshot เท่านั้น เว้นแต่คุณจะ `CONFIG SET appendonly yes` เอง (หรือตั้งใน `redis.conf`) ตั้งแต่ Redis 7.0 การเปิด AOF จะสร้างโครงสร้างแบบ multi-part ไว้ใน directory (`appenddirname` ค่าเริ่มต้นคือ `appendonlydir`): ไฟล์ base, ไฟล์ incremental หนึ่งไฟล์ขึ้นไป และ manifest ที่เชื่อมทั้งหมดเข้าด้วยกัน — นี่คือสิ่งที่ทำให้ background rewrite ทำได้ถูกกว่าไฟล์เดียวที่โตขึ้นเรื่อย ๆ แบบเดิม

</Callout>

## ข้อแลกเปลี่ยน

| ตัวเลือก | Benefit | Cost |
|---|---|---|
| `appendfsync always` | ไม่สูญหายข้อมูลแม้ crash | `fsync()` ทุกคำสั่ง ทำให้ throughput ต่ำที่สุดในสามโหมด |
| `appendfsync everysec` | สมดุลระหว่างความทนทานกับ throughput เหมาะกับ production ส่วนใหญ่ | สูญเสียข้อมูลได้สูงสุด ~1 วินาทีเมื่อ crash |
| `appendfsync no` | throughput สูงสุด ไม่มี overhead จาก fsync | OS ตัดสินใจเองว่าจะ flush เมื่อใด ข้อมูลสูญหายได้ไม่แน่นอนหากดิสก์ยังไม่ flush |

## ข้อผิดพลาดที่พบบ่อย

- **คิดว่า replica ทดแทนความจำเป็นของ persistence ได้** — replica ที่ crash และ restart ก็ต้องโหลดจากไฟล์ persistence ของตัวเอง (หรือ sync ใหม่จาก primary) ควรเปิด persistence บน replica แยกต่างหาก ไม่ใช่พึ่งพา primary เพียงอย่างเดียว
- **ใช้ `appendfsync no` ในระบบที่ทนการสูญหายข้อมูลไม่ได้** — โหมดนี้ให้ throughput สูงสุดแต่ปล่อยให้ OS ตัดสินใจเวลา flush เอง หาก crash ก่อน OS flush ข้อมูลตั้งแต่ flush ล่าสุดจะหายทั้งหมด
- **ไม่ทดสอบ recovery หลัง crash จริง** — การตั้งค่า `appendfsync` ที่ถูกต้องไม่มีประโยชน์ถ้าไม่เคยลองทำ `kill -9` แล้วดูว่า Redis restart และโหลดข้อมูลกลับมาได้ตามที่คาดไว้จริงหรือไม่

> 💡 ตัวอย่างจากของจริง
>
> **หลักการทั่วไป** — ทีมที่ดูแล Redis instance ที่เก็บข้อมูลสำคัญมักตั้ง `appendfsync everysec` เป็นค่ามาตรฐาน เพื่อรักษาสมดุลระหว่าง durability กับ write throughput บน workload ที่มีการเขียนหนัก
>
> **Financial-grade queue systems** — ระบบที่เกี่ยวกับธุรกรรมทางการเงินมักเลือก `appendfsync always` แม้ throughput จะต่ำลง เพราะยอมรับการสูญหายข้อมูลแม้แต่คำสั่งเดียวไม่ได้

export const quizDurability = [
  {
    q: "โหมด `appendfsync` ใดที่รับประกันว่าจะไม่มีการสูญหายของข้อมูลเมื่อเกิด crash?",
    options: ["no", "everysec", "always", "rdb"],
    answer: 2,
  },
  {
    q: "โหมด `appendfsync` ใดที่แนะนำสำหรับการ deploy ใน production ส่วนใหญ่?",
    options: ["always", "everysec", "no", "rdbsync"],
    answer: 1,
  },
  {
    q: "การสูญหายของข้อมูลสูงสุดเมื่อใช้ `appendfsync everysec` คือเท่าใด?",
    options: ["0 คำสั่ง", "~1 วินาทีของการเขียน", "ตั้งแต่ snapshot ล่าสุด", "ตั้งแต่ OS flush ล่าสุด"],
    answer: 1,
  },
  {
    q: "การรัน Redis replica แทนที่ความจำเป็นของ persistence บนแต่ละ node หรือไม่?",
    options: [
      "ใช่ replica มี copy ครบเสมอ",
      "ไม่ ควรเปิดใช้ persistence บน replica อย่างอิสระ",
      "เฉพาะเมื่อ replica อยู่ใน datacenter เดียวกัน",
      "ใช่ ตราบใดที่มี replica อย่างน้อยสอง node",
    ],
    answer: 1,
  },
];

<Quiz client:visible id="persistence-expiration/durability" questions={quizDurability} />

<ProgressTracker client:visible id="persistence-expiration/durability" />
